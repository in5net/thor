expr            : term (('+' | '-') term)*
                : 'let' identifier '=' expr

term            : factor (('+' | '-') factor)*

factor          : ('+' | '-') factor
                : power

power           : atom ('^' atom)*

atom            : '(' expr ')'
                : NUMBER
                : IDENTIFIER